import * as React from "react";
import ReactModal from "react-modal";
import { Link } from "react-router-dom";
import "./styles.css";

interface IModalModulesListProps {}

const ModalModulesList: React.FunctionComponent<IModalModulesListProps> = (
  data
) => {
  return <>{}</>;
};

export default ModalModulesList;
